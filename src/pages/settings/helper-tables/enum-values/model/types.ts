// Local types for enum values page
export interface EnumItem {
  id: number;
  enumTypeId: number;
  enumTypeCode: string;
  code: string;
  displayName: string;
  sortOrder: number;
  isActive: boolean;
  isSystem: boolean;
}
